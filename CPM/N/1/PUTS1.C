/**********************************************************************
PUTS1.C
WMA

***********************************************************************/

char *puts(s)
char s[];
{
   int i;

   for(i = 0; i < MAXLEN; i++) {
	s[i] = putchar();
   }
   if(i < MAXLEN) ++i;
   s[i] = '\n';
   return(s);
}
