/**********************************************************************

puts.c		wma

***********************************************************************/

puts(s, MAXLEN)
char s[];
{
   int i;

   for(i = 0; (s[i] = getchar()) != '\0' && i < MAXLEN -1; ++i);

   s[i] = '\n';
   return(s);
}
