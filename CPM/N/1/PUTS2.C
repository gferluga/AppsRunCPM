/**********************************************************************

puts2.c		wma

***********************************************************************/

puts(s, MAXLEN)
char s[];
{
   int i;

   for(i = 0; (s[i] = putchar()) != '\0' && i < MAXLEN -1; ++i);

   s[i] = '\n';
   return(s);
}
